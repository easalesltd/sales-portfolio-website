'use client';

import ShowcaseSlideshow from '../ShowcaseSlideshow';

/** Client-only hero slideshow (LCP lives here). */
export default function HomeHeroSlideshow() {
  return <ShowcaseSlideshow />;
}
